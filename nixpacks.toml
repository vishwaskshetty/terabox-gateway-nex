[phases.setup]
nixPkgs = ["playwright-driver.browsers"]

[phases.install]
cmds = [
    "pip install -r requirements.txt",
    "playwright install chromium --with-deps || true"
]

[start]
cmd = "gunicorn main:app --bind 0.0.0.0:$PORT --workers 1 --threads 8 --timeout 180"
